{
  "name": "@stacksport-example/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI to inspect StacksPort DCA vaults — read-only, no wallet needed.",
  "type": "module",
  "bin": {
    "stacksport-dca": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@stacksport/dca-sdk": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
