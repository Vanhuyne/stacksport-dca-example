{
  "name": "@stacksport-example/telegram-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram bot that announces StacksPort DCA executions in real-time. Polls vault stats and posts on delta.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dry-run": "node dist/index.js dry-run",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@stacksport/dca-sdk": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^6"
  },
  "engines": {
    "node": ">=20"
  }
}
